# WPCRM — Docker Compose para Easypanel / VPS
# Suba esse arquivo em um Stack no Easypanel OU rode localmente com: docker compose up -d

services:
  database:
    image: postgres:15-alpine
    ports:
      - "5438:5432"
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-presidente}
      POSTGRES_USER: ${POSTGRES_USER:-wpcrm_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-wpcrm_pass_secure}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-wpcrm_user} -d ${POSTGRES_DB:-presidente}" ]
      interval: 10s
      timeout: 5s
      retries: 5

  wpcrm:
    build: .
    ports:
      - "${APP_PORT:-9020}:9020"
    depends_on:
      database:
        condition: service_healthy
    environment:
      - PORT=9020
      - DATABASE_URL=postgresql://${POSTGRES_USER:-wpcrm_user}:${POSTGRES_PASSWORD:-wpcrm_pass_secure}@database:5432/${POSTGRES_DB:-presidente}
      - JWT_SECRET=${JWT_SECRET}
      - WAHA_API_URL=${WAHA_API_URL}
      - WAHA_API_KEY=${WAHA_API_KEY}
      - DB_PATH=/app/data/db.json
    volumes:
      - wpcrm_data:/app/data
    restart: unless-stopped

volumes:
  postgres_data:
  wpcrm_data:
